#include<iostream>
#include<vector>
#include<string>

using namespace std;

class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) 
    {
        string common;
        auto temp1=*ptr1.begin();
        auto temp2=*ptr2.begin();
        for(auto ptr1=strs.begin();ptr1!=strs.end();ptr1++)
        {
            for(auto ptr2=ptr1+1;ptr1!=strs.end();ptr2++)
            {
                
            }
        }
    }
};